import Ember from 'ember';

/**
 * Component that creates a text box for live filtering with filter icon inside
 */
export default Ember.Component.extend({
  placeholder: undefined,
  keyUp: undefined,
  value: undefined,
});
